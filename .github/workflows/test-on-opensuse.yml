name: Test Nipe on OpenSUSE

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-opensuse:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run OpenSUSE smoke test
        run: ./.github/scripts/run-distro-smoke-test.sh opensuse
