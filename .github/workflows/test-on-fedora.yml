name: Test Nipe on Fedora

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-fedora:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run Fedora smoke test
        run: ./.github/scripts/run-distro-smoke-test.sh fedora
